<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:background="@drawable/sng_rank_bg"
    android:layout_height="match_parent"
    tools:context=".loading">

    <ImageView
        android:id="@+id/icon"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="100dp"
        android:src="@drawable/loading_logo_eng" />

    <io.saeid.fabloading.LoadingView
        android:layout_marginBottom="5dp"
        android:layout_marginTop="40dp"
        android:layout_above="@+id/imgVPPLover"
        android:layout_centerInParent="true"
        android:id="@+id/loading_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:scaleX="1.2"
        android:scaleY="1.2"
        android:padding="2dp"
        app:mfl_duration="280"
        app:mfl_repeat="20"/>

    <!--<TextView-->
    <!--android:layout_marginTop="20dp"-->
    <!--android:layout_centerInParent="true"-->
    <!--android:id="@+id/ttt"-->
    <!--android:textColor="@color/whiteicon"-->
    <!--android:textSize="20dp"-->
    <!--android:layout_below="@id/loading_view"-->
    <!--android:textAlignment="center"-->
    <!--android:text="Loading ..."-->
    <!--android:layout_width="wrap_content"-->
    <!--android:layout_height="wrap_content" />-->

    <TextView
        android:textAlignment="center"
        android:textColor="@color/colorWhite"
        android:id="@+id/imgVPPLover"
        android:padding="5dp"
        android:layout_centerInParent="true"
        android:layout_marginBottom="70dp"
        android:layout_alignParentBottom="true"
        android:text="Loading ..."
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

</RelativeLayout>